/target
contracts/target
**/*.rs.bk
.env
.env.*
!.env.example
*.wasm

# Regenerated by `cargo test` on every run; deterministic, so committing them
# only adds churn to diffs.
contracts/**/test_snapshots/
/deployments/*.json
!/deployments/.gitkeep
node-key.json
node-key.pem
.DS_Store
/data
