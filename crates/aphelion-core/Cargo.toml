[package]
name = "aphelion-core"
description = "Shared domain types, fixed-point price math and canonical signing payloads for the Aphelion oracle network."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
serde = { workspace = true }
thiserror = { workspace = true }
hex = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
# Test-only: the beacon vectors pin the commitment, which is the SHA-256 of the
# preimage this crate builds. The crate itself hashes nothing -- the node does
# the hashing and the contract checks it -- so this stays out of the public
# dependency set.
sha2 = { workspace = true }
